# Dependencias minimas del backend para el deploy del dashboard.
# NO incluye acryl-datahub ni langchain: el backend solo sirve el historico
# desde SQLite; los agentes que hablan con DataHub corren aparte, en local.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
