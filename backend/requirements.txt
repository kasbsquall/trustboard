# Minimal backend dependencies for the dashboard deploy.
# acryl-datahub is deliberately absent: the backend only serves the history
# from SQLite. The agents that talk to DataHub run separately.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
