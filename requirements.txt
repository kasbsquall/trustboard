# TrustBoard - Python dependencies. Requires Python 3.11+.

# --- DataHub ---
# Official SDK: DataHubGraph to read aspects and run GraphQL (structured
# properties, tags, incidents).
acryl-datahub==1.6.0.6
# Note: mcp-server-datahub is NOT installed here. It runs through
# `uvx mcp-server-datahub@latest` for interactive exploration and carries its
# own acryl-datahub pin that would clash with the one above.

# --- MCP ---
# TrustBoard publishes the trust score to other agents over MCP.
mcp==1.12.4

# --- Backend ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Utilities ---
python-dotenv==1.0.1
httpx==0.28.1
requests==2.32.3

# --- Tests ---
pytest==8.3.4
pytest-cov==6.0.0

# TypedDict for the MCP tool schemas: pydantic rejects typing.TypedDict below 3.12.
typing_extensions>=4.9

# The Navigator agent (agents/navigator.py) is the only thing here that calls a
# model. Everything else runs without it.
anthropic>=0.40
